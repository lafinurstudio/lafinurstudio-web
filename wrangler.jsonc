export default {
  async fetch(request) {
    const url = new URL(request.url);
    url.hostname = 'lafinurstudio-web.mentearquitectura.workers.dev';
    
    return fetch(new Request(url, request));
  }
};
